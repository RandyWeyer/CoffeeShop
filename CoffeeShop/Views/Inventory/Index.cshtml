@{
  Layout = "_Layout";
}

<h1>Our current inventory:</h1>
<hr />

@if (Model.Count != 0)
{
  <ol>
    @foreach (var drink in Model)
    {
      <li>@drink.GetItem(), @drink.GetItemAmount()</li>
    }
  </ol>
}
