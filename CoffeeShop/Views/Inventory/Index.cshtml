@{
  Layout = "_Layout";
}

<h1>Our current inventory:</h1>
<hr />

@if (Model.Count != 0)
{
  <ol>
    @foreach (var item in Model)
    {
      <li>@item.GetItem(), @item.GetItemAmount()</li>
    }
  </ol>
}
